{
  "private": true,
  "description": "e2e template test",
  "scripts": {
    "build": "npm --prefix roadmap-demo ci && npm --prefix roadmap-demo run build && node -e \"const fs=require('fs'); fs.rmSync('dist',{recursive:true,force:true}); fs.cpSync('roadmap-demo/dist','dist',{recursive:true})\""
  }
}
